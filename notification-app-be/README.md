# Notification Backend

This project fetches notifications from the API and displays the top 10 notifications based on priority.

Priority Order:

- Placement
- Result
- Event

If two notifications have the same priority, the latest notification is shown first.